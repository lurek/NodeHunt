# Customization

Update public site settings in `src/config/site.ts`, advertising controls in `src/config/ads.ts`, and visual tokens in `src/styles/global.css`. Ads remain disabled by default. Any new external script must be evaluated for CSP, privacy, accessibility, and performance before it is added.
